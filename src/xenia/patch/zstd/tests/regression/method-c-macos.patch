--- a/third_party/zstd/tests/regression/method.c
+++ b/third_party/zstd/tests/regression/method.c
@@ -14,7 +14,11 @@
 #include <stdlib.h>
 
 #define ZSTD_STATIC_LINKING_ONLY
+#ifdef __APPLE__
+#include "zstd.h"
+#else
 #include <zstd.h>
+#endif
 
 #define MIN(x, y) ((x) < (y) ? (x) : (y))
 
