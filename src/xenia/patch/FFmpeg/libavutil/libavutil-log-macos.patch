--- a/third_party/FFmpeg/libavutil/log.c
+++ b/third_party/FFmpeg/libavutil/log.c
@@ -176,7 +176,12 @@ static void check_color_terminal(void)
 #endif
     }
 
+#ifdef __APPLE__
+    /* On macOS/Clang, need explicit parentheses for logical operator precedence */
+    if (getenv("AV_LOG_FORCE_256COLOR") || (term && strstr(term, "256color")))
+#else
     if (getenv("AV_LOG_FORCE_256COLOR") || term && strstr(term, "256color"))
+#endif
         use_color *= 256;
 }
 
@@ -383,6 +388,16 @@ void av_log_default_callback(void* ptr, int level, const char* fmt, va_list vl)
         count = 0;
     }
     strcpy(prev, line);
+#ifdef __APPLE__
+    sanitize((uint8_t *)part[0].str);
+    colored_fputs(type[0], 0, part[0].str);
+    sanitize((uint8_t *)part[1].str);
+    colored_fputs(type[1], 0, part[1].str);
+    sanitize((uint8_t *)part[2].str);
+    colored_fputs(av_clip(level >> 3, 0, NB_LEVELS - 1), tint >> 8, part[2].str);
+    sanitize((uint8_t *)part[3].str);
+    colored_fputs(av_clip(level >> 3, 0, NB_LEVELS - 1), tint >> 8, part[3].str);
+#else
     sanitize(part[0].str);
     colored_fputs(type[0], 0, part[0].str);
     sanitize(part[1].str);
@@ -391,6 +406,7 @@ void av_log_default_callback(void* ptr, int level, const char* fmt, va_list vl)
     colored_fputs(av_clip(level >> 3, 0, NB_LEVELS - 1), tint >> 8, part[2].str);
     sanitize(part[3].str);
     colored_fputs(av_clip(level >> 3, 0, NB_LEVELS - 1), tint >> 8, part[3].str);
+#endif
 
 #if CONFIG_VALGRIND_BACKTRACE
     if (level <= BACKTRACE_LOGLEVEL)
