--- a/third_party/zlib-ng/arch/x86/adler32_avx512.c
+++ b/third_party/zlib-ng/arch/x86/adler32_avx512.c
@@ -12,6 +12,11 @@
 #include "adler32_p.h"
 #include "arch_functions.h"
 #include <immintrin.h>
+
+#pragma GCC target("avx512f,avx512vl")
+#if defined(__clang__)
+#pragma clang attribute push(__attribute__((target("avx512f,avx512vl"))), apply_to=function)
+#endif
 #include "x86_intrins.h"
 #include "adler32_avx512_p.h"
 
@@ -101,8 +106,12 @@
 }
 
 Z_INTERNAL uint32_t adler32_avx512(uint32_t adler, const uint8_t *src, size_t len) {
-    return adler32_fold_copy_impl(adler, NULL, src, len, 0);
+    return adler32_fold_copy_impl(adler, NULL, src, len, 0);
 }
 
+#if defined(__clang__)
+#pragma clang attribute pop
 #endif
 
+#endif
+
