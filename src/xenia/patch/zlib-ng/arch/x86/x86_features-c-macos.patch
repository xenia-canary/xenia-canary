--- a/third_party/zlib-ng/arch/x86/x86_features.c
+++ b/third_party/zlib-ng/arch/x86/x86_features.c
@@ -57,8 +57,15 @@
 }
 
 static inline uint64_t xgetbv(unsigned int xcr) {
-#if defined(_MSC_VER) || defined(X86_HAVE_XSAVE_INTRIN)
+#if defined(_MSC_VER)
     return _xgetbv(xcr);
+#elif defined(__clang__) && defined(X86_HAVE_XSAVE_INTRIN)
+    // Use inline assembly to avoid target feature requirements
+    uint32_t eax, edx;
+    __asm__ __volatile__ (".byte 0x0f, 0x01, 0xd0" : "=a"(eax), "=d"(edx) : "c"(xcr));
+    return (uint64_t)(edx) << 32 | eax;
+#elif defined(X86_HAVE_XSAVE_INTRIN)
+    return _xgetbv(xcr);
 #else
     uint32_t eax, edx;
     __asm__ ( ".byte 0x0f, 0x01, 0xd0" : "=a"(eax), "=d"(edx) : "c"(xcr));
